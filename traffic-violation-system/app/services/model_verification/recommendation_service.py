class RecommendationService:
    @staticmethod
    def get_recommendations() -> list:
        return [
            "Model running normally. CPU / GPU loads are within nominal thresholds.",
            "GPU hardware acceleration recommended for optimal parallel stream pipelines.",
            "Validation dataset split checked. Split partitions correspond to standard standards."
        ]
